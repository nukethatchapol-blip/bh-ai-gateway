-- BEARHOUSE AI Gateway seed.
-- Branches are NOT seeded — public.branches is a view over the real
-- public.branch table (85 rows). Skills are seeded inside 0001_init.sql
-- so this file is intentionally empty.
select 1;
